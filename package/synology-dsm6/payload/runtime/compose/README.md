# Compose Runtime

Здесь будет лежать package-specific compose layer для режима Synology package.

Он должен быть отделен от корневого `docker-compose.yml`, который остается baseline для локальной разработки и текущего ops workflow.
