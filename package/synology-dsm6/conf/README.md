# Conf

Этот каталог предназначен для package framework config:
- resource declarations
- port registration
- другие package-level конфиги, не входящие в `package.tgz`

Для initial DSM6 scope `conf/privilege` не считается обязательным.
